<nav class="navbar navbar-expand-lg navbar-light bg-light  fixed-top">
				<div class="container">
					<a class="navbar-brand" href="#"><img src="{{ asset('image/logo.jpg') }}" width="35px" alt='logo'><strong class="ms-1 i">I</strong>-<strong class="tsena">tsena</strong>
					</a>
					<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
						<span class="navbar-toggler-icon"></span>
					</button>
					<div class="collapse navbar-collapse" id="navbarSupportedContent">
						<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
							<li class="nav-item">
								{% if app.request.attributes.get('_route') == 'app_home'%}
									<a class="nav-link active" aria-current="page" href="{{path('app_home')}}">Home</a>
								{% else %}
									<a class="nav-link " aria-current="page" href="{{path('app_home')}}">Home</a>
								{% endif %}
							</li>
							<li class="nav-item">
								{% if app.request.attributes.get('_route') == 'app_product'%}
									<a class="nav-link active" href="{{path('app_product')}}">Products</a>
								{% else %}
									<a class="nav-link" href="{{path('app_product')}}">Products</a>
								{% endif %}
							</li>
							<li class="nav-item">
								{% if app.request.attributes.get('_route') == 'app_contact'%}
									<a class="nav-link active" href="{{path('app_contact')}}">Contacts Us</a>
								{% else %}
									<a class="nav-link" href="{{path('app_contact')}}">Contacts Us</a>
								{% endif %}
							</li>
							<li class="nav-item">
								{% if app.request.attributes.get('_route') == 'app_cart'%}
									<a class="nav-link active" href="{{path('app_cart')}}">
								<i class="fa fa-shopping-cart"></i>
								<span class="badge">{{panier | length}}</span>
							</a>
								{% else %}
								<a class="nav-link" href="{{path('app_cart')}}">
							<i class="fa fa-shopping-cart"></i>
							<span class="badge">{{panier | length}}</span>
						</a>
								{% endif %}

							</li>

							{% if app.user == "" %}
							<li class="nav-item">
							{% if app.request.attributes.get('_route') == 'app_login'%}
							<a class="nav-link active" href="{{path('app_login')}}">
						<i class="fa fa-user"></i>
					</a>
					{% else %}
					<a class="nav-link" href="{{path('app_login')}}">
				<i class="fa fa-user"></i>
			</a>
					{% endif %}

							</li>
                            {% endif %}

							{% if app.user %}
							<li class="nav-item">
								<a class="nav-link" href="{{ path('app_logout') }}">
									<i class="fa fa-power-off"></i>
								</a>
							</li>
							{% endif %}
						</ul>
					</div>
				</div>
			</nav>